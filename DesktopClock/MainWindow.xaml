<Window x:Class="DesktopClock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:utilities="clr-namespace:DesktopClock.Utilities"
        xmlns:p="clr-namespace:DesktopClock.Properties"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="DesktopClock"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Opacity="{Binding WindowOpacity, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        Topmost="{Binding Topmost, Source={x:Static p:Settings.Default}, Mode=TwoWay}"
        FontFamily="{Binding FontFamily, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        FontStyle="{Binding FontStyle, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        MouseDown="Window_MouseDown"
        SourceInitialized="Window_SourceInitialized"
        ContentRendered="Window_ContentRendered"
        Closing="Window_Closing"
        StateChanged="Window_StateChanged">
    <Window.Background>
        <SolidColorBrush Color="Transparent" />
    </Window.Background>

    <Window.Resources>
        <utilities:ColorCollectionToGradientConverter x:Key="ColorCollectionToGradientConverter" />
        <utilities:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

        <ContextMenu x:Key="MainContextMenu"
                     x:Shared="False">
            <MenuItem Header="Click-_Through"
                      IsCheckable="True"
                      IsChecked="{Binding ClickThrough, Source={x:Static p:Settings.Default}, Mode=TwoWay}" />

            <MenuItem Command="{Binding ExitCommand}"
                      Header="E_xit" />
        </ContextMenu>

        <tb:TaskbarIcon x:Key="TrayIcon"
                        x:Shared="False"
                        IconSource="DesktopClock.ico"
                        ToolTipText="DesktopClock"
                        MenuActivation="LeftOrRightClick" />
    </Window.Resources>

    <Border CornerRadius="10" Padding="12,0,12,2">
        <Border.Background>
            <Binding Path="BackgroundGradientColors" Converter="{StaticResource ColorCollectionToGradientConverter}" />
        </Border.Background>
        <Viewbox Height="{Binding Height, Source={x:Static p:Settings.Default}, Mode=OneWay}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding CurrentTimeOrCountdownString}"
                           TextWrapping="Wrap">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{Binding EffectiveTextColor, Source={x:Static p:Settings.Default}, Mode=OneWay}" />
                    </TextBlock.Foreground>
                </TextBlock>
                <TextBlock Text="{Binding NotificationText}"
                           Margin="4,-1.75,-1,0"
                           TextWrapping="Wrap"
                           Visibility="{Binding NotificationText, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="{Binding EffectiveTextColor, Source={x:Static p:Settings.Default}, Mode=OneWay}" />
                    </TextBlock.Foreground>
                </TextBlock>
            </StackPanel>
        </Viewbox>
    </Border>
</Window>
